const express = require('express'); 
const app = express(); 
app.get('/', (req, res) =
    res.send('RAYGAME Server is running!'); 
}); 
app.listen(3000, () =
    console.log('RAYGAME started on port 3000'); 
}); 
